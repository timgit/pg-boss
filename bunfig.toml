# checkDuplicateTestNames must run before hooks.ts so a name collision fails fast.
[test]
preload = ["./test/checkDuplicateTestNames.ts", "./test/hooks.ts"]
coverageSkipTestFiles = true
# Coverage is a src/ metric — keep the test helpers and scripts out of the report.
coveragePathIgnorePatterns = ["test/**", "scripts/**"]
